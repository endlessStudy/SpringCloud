spring:
  application:
    name: eureka-client-provide2
server:
  port: 2230
eureka:
  client:
    serviceUrl:
      defaultZone: http://root:root@localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
#  instance:
#    instance-id: eureka-client-provide